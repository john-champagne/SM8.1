// Defines the preprocessor module of the assembler.
// The preprocessor is in charge of removing whitespace and comments.

#ifndef PREPROCESSOR_H
#define PREPROCESSOR_H

char* alloc_string(char*);
char* preprocess_line(char*);

#endif
